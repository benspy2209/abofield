// This file is now deprecated and moved to the navbar folder structure
// We're keeping it to make sure it doesn't get referenced, but this should be removed when all imports are updated
export { default } from './navbar';
